version: '3.5'

services:
  postgres:
    image: postgres:10
    volumes:
      - ./data:/var/lib/postgresql/data
    expose:
      - 5432

  app:
    build:
      context: app
    image: finepanel:latest
    depends_on:
      - postgres
      - sidekiq
      - redis

  redis:
    image: redis:latest
    expose:
      - 6379

  sidekiq:
    build:
      context: app
    image: finepanel:latest
    depends_on:
      - redis
      - postgres
    env_file:
      - app/.env
